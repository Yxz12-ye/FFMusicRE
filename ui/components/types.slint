export struct TagData {
    label: string,
    accent: bool,
}

export struct ModeOption {
    label: string,
    active: bool,
}

export struct QueueTrack {
    order: string,
    title: string,
    artist: string,
    duration: string,
    active: bool,
}

export struct LyricLineData {
    text: string,
    active: bool,
    muted: bool,
}
